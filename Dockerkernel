# syntax=docker/dockerfile:1

FROM sigmabase AS kernel
WORKDIR /home/sigmaos
ENV kernelid kernel
ENV named :1111
ENV boot named
ENV dbip x.x.x.x
ENV ports 0-0
CMD ["sh", "-c", "bin/linux/bootkernel ${kernelid} ${named} ${boot} ${dbip} ${ports}"]
