# syntax=docker/dockerfile:1

FROM sigmaosbase AS kernel

WORKDIR /home/sigmaos
RUN mkdir .aws && chmod 700 .aws
COPY aws/.aws/credentials .aws/credentials
RUN chmod 600 .aws/credentials
CMD ["bin/linux/bootkernel", "bootkernelclnt/bootall.yml"]
